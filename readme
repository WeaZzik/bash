# Download and Execute Script #
cd /tmp
wget https://raw.githubusercontent.com/WeaZzik/bash/main/lamp.sh
chmod +u+r+x lamp.sh
./lamp.sh

# Add HTTPS to a custom URL #
sudo certbot --apache -d <URL> --post-hook "/usr/sbin/service apache2 restart"
